---
import Star from '@/assets/icons/star.astro'

const testimonials = [
  {
    name: 'Laura Martínez',
    role: 'Ejecutiva',
    image: '',
    content:
      'Gracias a la Dra. Monica logré perder 15 kg de manera saludable. Su enfoque personalizado hizo toda la diferencia. ¡Altamente recomendada!',
    rating: 5,
  },
  {
    name: 'Carlos Rodríguez',
    role: 'Deportista',
    image: '',
    content:
      'El plan nutricional deportivo mejoró significativamente mi rendimiento. Monica entiende las necesidades de los atletas y adapta todo perfectamente.',
    rating: 5,
  },
  {
    name: 'Ana Sánchez',
    role: 'Mamá de 2',
    image: '',
    content:
      'Después del embarazo, Monica me ayudó a recuperar mi salud y energía. Sus planes son fáciles de seguir y deliciosos. ¡Gracias por todo!',
    rating: 5,
  },
]
---

<section id="testimonios" class="py-16 md:py-24 bg-background-muted">
  <div class="container mx-auto px-4">
    <div class="text-center space-y-4 mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-balance">
        Lo que Dicen Mis Pacientes
      </h2>
      <p class="text-lg text-text-muted max-w-2xl mx-auto text-pretty">
        La satisfacción de mis pacientes es mi mayor motivación. Lee sus
        historias de transformación.
      </p>
    </div>

    <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
      {
        testimonials.map((testimonial) => (
          <div class="p-6 bg-card border-text-muted rounded-2xl shadow-sm">
            <div class="space-y-4">
              <div class="flex gap-1">
                {[...Array(testimonial.rating)].map((_, i) => (
                  <span class="text-primary fill-primary">
                    <Star filled={true} />
                  </span>
                ))}
              </div>

              <p class="text-text-muted leading-relaxed text-pretty">
                "{testimonial.content}"
              </p>

              <div class="flex items-center gap-3 pt-4 border-t border-text-muted">
                <div class="h-12 w-12 rounded-full overflow-hidden bg-secondary shrink-0">
                  <img
                    src={testimonial.image || '/placeholder.avif'}
                    alt={testimonial.name}
                    class="w-full h-full object-cover"
                  />
                </div>
                <div>
                  <div class="font-semibold text-text">{testimonial.name}</div>
                  <div class="text-sm text-text-muted">{testimonial.role}</div>
                </div>
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
